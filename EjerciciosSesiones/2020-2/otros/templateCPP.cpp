#include <bits/stdc++.h>

using namespace std;
#define watch(x) cout << (#x) << " es " << x << endl;
#define EPS 1.19209e-07
#define fastIO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
typedef int upiiz;
typedef long long int lli;
typedef vector<upiiz> vupiiz;
typedef vector<double> vd;
#define for(n) for(int i = 0; i < n; i++)
lli MAX = 1e9+7;

int main() {
    fastIO
    vupiiz arreglo1, arreglo2;
    int segundos = 3600;
    bool luces[10] = {0};
    cout << "hola" << endl;
    watch(segundos)
    for(4) {
        cout << i << endl;
    }
    auto sumar = [&](int a, int b) {
        return a + b;
    };

    cout << sumar(6, 5) << endl;

    return 0;
}
